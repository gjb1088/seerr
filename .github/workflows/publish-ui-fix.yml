name: Publish Seerr UI Fix

on:
  push:
    branches:
      - v3.4.1-custom
    paths:
      - '.github/workflows/publish-ui-fix.yml'

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit timestamp
        id: ts
        shell: bash
        run: echo "TIMESTAMP=$(git log -1 --pretty=%ct)" >> "$GITHUB_OUTPUT"

      - name: Build and push custom Seerr image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          build-args: |
            COMMIT_TAG=31beb8b65c756e9a4da7002aef6607d1d314b6f9
            BUILD_VERSION=3.4.1-episode-requests-seeded-ui-fix
            SOURCE_DATE_EPOCH=${{ steps.ts.outputs.TIMESTAMP }}
          tags: |
            ghcr.io/gjb1088/seerr:3.4.1-episode-requests-seeded-ui-fix
            ghcr.io/gjb1088/seerr:31beb8b65c756e9a4da7002aef6607d1d314b6f9
          labels: |
            org.opencontainers.image.title=Seerr
            org.opencontainers.image.description=Seerr v3.4.1 with individual episode requests, seed-aware selection, timeout fix, and Request More UI fix
            org.opencontainers.image.source=https://github.com/gjb1088/seerr
            org.opencontainers.image.revision=31beb8b65c756e9a4da7002aef6607d1d314b6f9
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: false
