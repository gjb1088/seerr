name: Publish Seerr Episode Availability Fix

on:
  push:
    branches:
      - v3.4.1-custom
    paths:
      - '.github/workflows/publish-episode-availability-fix.yml'

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit timestamp
        id: ts
        shell: bash
        run: echo "TIMESTAMP=$(git log -1 --pretty=%ct)" >> "$GITHUB_OUTPUT"

      - name: Build and push custom Seerr image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          build-args: |
            COMMIT_TAG=33cf355317bf67b6afc409623e86971d187f1e42
            BUILD_VERSION=3.4.1-episode-requests-seeded-availability-fix
            SOURCE_DATE_EPOCH=${{ steps.ts.outputs.TIMESTAMP }}
          tags: |
            ghcr.io/gjb1088/seerr:3.4.1-episode-requests-seeded-availability-fix
            ghcr.io/gjb1088/seerr:33cf355317bf67b6afc409623e86971d187f1e42
          labels: |
            org.opencontainers.image.title=Seerr
            org.opencontainers.image.description=Seerr v3.4.1 with individual episode requests, seed-aware selection, timeout fix, request-more UI fix, and episode availability gating
            org.opencontainers.image.source=https://github.com/gjb1088/seerr
            org.opencontainers.image.revision=33cf355317bf67b6afc409623e86971d187f1e42
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: false
